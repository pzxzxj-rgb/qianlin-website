# ERP provider placeholder

This directory is reserved for a future provider adapter. No endpoint, credential, request field, response field, or tenant identifier is defined until the ERP vendor supplies formal API documentation and a test environment.
